<h1>号車選択</h1>

<p>
  <strong><%= @run.name %></strong>
</p>

<ul>
  <% @cars.each do |car| %>
    <li>
      <%= link_to "#{car.number}号車（#{car.car_type.name}）",
          run_car_seats_path(@run, car_id: car.id) %>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to "検索結果に戻る", runs_path %>
</p>
